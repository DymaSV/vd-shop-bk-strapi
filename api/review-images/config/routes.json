{
  "routes": [
    {
      "method": "GET",
      "path": "/review-images",
      "handler": "review-images.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/review-images/count",
      "handler": "review-images.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/review-images/:id",
      "handler": "review-images.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/review-images",
      "handler": "review-images.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/review-images/:id",
      "handler": "review-images.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/review-images/:id",
      "handler": "review-images.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
